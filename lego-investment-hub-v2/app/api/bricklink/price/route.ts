import {NextRequest,NextResponse} from "next/server";
import OAuth from "oauth-1.0a";
import crypto from "crypto";
export async function GET(req:NextRequest){const setNumber=req.nextUrl.searchParams.get("setNumber")?.trim(),condition=req.nextUrl.searchParams.get("condition")==="U"?"U":"N";if(!setNumber)return NextResponse.json({error:"Missing setNumber"},{status:400});const consumerKey=process.env.BRICKLINK_CONSUMER_KEY,consumerSecret=process.env.BRICKLINK_CONSUMER_SECRET,token=process.env.BRICKLINK_TOKEN,tokenSecret=process.env.BRICKLINK_TOKEN_SECRET;if(!consumerKey||!consumerSecret||!token||!tokenSecret)return NextResponse.json({error:"BrickLink credentials are not configured yet. Add the four BRICKLINK_* values in .env.local."},{status:503});const oauth=new OAuth({consumer:{key:consumerKey,secret:consumerSecret},signature_method:"HMAC-SHA1",hash_function(base,key){return crypto.createHmac("sha1",key).update(base).digest("base64")}});const no=setNumber.includes("-")?setNumber:`${setNumber}-1`;const url=`https://api.bricklink.com/api/store/v1/items/SET/${encodeURIComponent(no)}/price?guide_type=sold&new_or_used=${condition}&currency_code=USD`;const request={url,method:"GET"};const auth=oauth.toHeader(oauth.authorize(request,{key:token,secret:tokenSecret}));const r=await fetch(url,{headers:{Authorization:auth.Authorization},cache:"no-store"});const body=await r.json();if(!r.ok||body.meta?.code!==200)return NextResponse.json({error:body.meta?.message||"BrickLink price request failed"},{status:r.status||500});return NextResponse.json(body.data);}
